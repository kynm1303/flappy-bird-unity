using System;
public static class CircleCircleColliderStategy
{
    public static bool isCollide(MyCircleCollider circle1, MyCircleCollider circle2)
    {
        // implement later
        return false;
    }
}
